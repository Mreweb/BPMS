name: Deploy BPMS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DEPLOY_KEY }}

      - name: Deploy to Ubuntu Server
        run: |
          ssh -o StrictHostKeyChecking=no -p 1997 root@45.149.78.155 "
            cd /var/www/html/shamin_paziresh || exit 1
            git pull origin main || git clone git@github.com:Mreweb/BPMS.git
          "